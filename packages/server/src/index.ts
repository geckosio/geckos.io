import server, { ServerChannel } from './server'
import iceServers from './iceServers'
import { Data, RawMessage } from '@geckos.io/common/lib/typings'

export default server
export { ServerChannel, Data, RawMessage, iceServers }
